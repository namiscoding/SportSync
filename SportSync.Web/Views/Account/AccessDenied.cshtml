@{
    ViewData["Title"] = "Từ chối truy cập";
}

<style>
    .access-denied-container {
        text-align: center;
        padding-top: 50px;
        padding-bottom: 50px;
    }

    .access-denied-icon {
        font-size: 5rem;
        color: var(--bs-danger);
    }
</style>

<div class="container access-denied-container">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-sm border-danger">
                <div class="card-body p-5">
                    <div class="access-denied-icon mb-4">
                        <i class="fas fa-hand-paper"></i>
                    </div>
                    <h1 class="display-4 fw-bold text-danger">@ViewData["Title"]</h1>
                    <p class="lead text-muted mt-3">
                        Bạn không có quyền truy cập vào tài nguyên hoặc trang này.
                    </p>
                    <hr class="my-4">
                    <p>
                        Nếu bạn tin rằng đây là một sự nhầm lẫn, vui lòng liên hệ với quản trị viên hệ thống.
                    </p>
                    <a asp-action="Index" asp-controller="Home" class="btn btn-primary btn-lg mt-3">
                        <i class="fas fa-home me-2"></i> Quay về Trang chủ
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
